# 📊 Лимиты отображения данных

## Обзор

Для оптимизации производительности и удобства использования в проекте реализованы лимиты отображения данных на страницах с большим объемом информации.

## Реализованные лимиты

### 1. **Телеметрия (Telemetry)**
- **Страница**: `/telemetry`
- **Лимит по умолчанию**: 100 записей
- **Максимум**: 10,000 записей
- **Доступные варианты**: 100 / 500 / 1,000 / Все

**Параметр запроса**: `?max=<число>`

**Примеры**:
```
http://localhost/telemetry          # 100 записей (по умолчанию)
http://localhost/telemetry?max=500  # 500 записей
http://localhost/telemetry?max=10000 # Все записи
```

**Файлы**:
- Контроллер: `services/php-web/laravel-patches/app/Http/Controllers/TelemetryController.php`
- Представление: `services/php-web/laravel-patches/resources/views/telemetry.blade.php`

---

### 2. **OSDR Dataset**
- **Страница**: `/osdr`
- **Лимит по умолчанию**: 20 записей
- **Максимум**: 500 записей
- **Доступные варианты**: 20 / 50 / 100 / Все

**Параметр запроса**: `?max=<число>`

**Примеры**:
```
http://localhost/osdr          # 20 записей (по умолчанию)
http://localhost/osdr?max=50   # 50 записей
http://localhost/osdr?max=500  # Все записи
```

**Файлы**:
- Контроллер: `services/php-web/laravel-patches/app/Http/Controllers/OsdrController.php`
- Представление: `services/php-web/laravel-patches/resources/views/osdr.blade.php`

---

### 3. **ISS Trend Data**
- **Страница**: `/iss`
- **Лимит по умолчанию**: 50 записей
- **Максимум**: 1,000 записей
- **Доступные варианты**: 50 / 100 / 500 / Все

**Параметр запроса**: `?max=<число>`

**Примеры**:
```
http://localhost/iss          # 50 записей (по умолчанию)
http://localhost/iss?max=100  # 100 записей
http://localhost/iss?max=1000 # Все записи
```

**Файлы**:
- Контроллер: `services/php-web/laravel-patches/app/Http/Controllers/IssController.php`
- Представление: `services/php-web/laravel-patches/resources/views/iss.blade.php`

---

## Технические детали

### Реализация в контроллере

```php
// Пример из TelemetryController.php
$maxDisplay = max(1, min(10000, (int) $request->query('max', 100)));
$totalRows = count($data['rows'] ?? []);

// Ограничение массива данных
$displayedRows = array_slice($data['rows'] ?? [], 0, $maxDisplay);

return view('telemetry', [
    'telemetry' => $displayedRows,
    'total_count' => $totalRows,
    'displayed_count' => count($displayedRows),
    // ... другие данные
]);
```

### Валидация параметров

Все лимиты валидируются:
- **Минимум**: 1 запись
- **Максимум**: зависит от страницы
- **По умолчанию**: применяется, если параметр отсутствует

Функция `max(1, min(MAX_VALUE, (int) $request->query('max', DEFAULT)))` гарантирует:
1. Минимум 1 запись
2. Не превышает максимум
3. Использует значение по умолчанию

---

## UI элементы

### Информационная панель
Показывает текущее состояние:
```
Показано X из Y записей
[100] [500] [1000] [Все]
```

### Кнопки переключения
- Активная кнопка имеет класс `active`
- Кнопки формируют GET-запросы с параметром `?max=`

---

## Производительность

### Преимущества лимитов:

1. **Быстрая загрузка страниц**
   - Меньше данных для рендеринга
   - Быстрее работа DataTables/графиков

2. **Меньше нагрузки на сервер**
   - Снижение использования памяти
   - Быстрее обработка PHP

3. **Лучший UX**
   - Пользователь видит данные быстрее
   - Возможность постепенного увеличения лимита

### Рекомендации:

- Для обычного просмотра: используйте лимиты по умолчанию
- Для анализа: увеличивайте лимит постепенно
- Для экспорта: используйте функцию экспорта, а не "Все записи"

---

## API Endpoints (Rust)

Если нужны данные через API без лимитов, используйте прямые endpoints:

```bash
# ISS последние данные
curl http://localhost:3000/last

# ISS тренд (без лимита)
curl http://localhost:3000/iss/trend

# OSDR данные
curl http://localhost:3000/osdr/datasets
```

---

## Будущие улучшения

### Планируется добавить:

1. **Пагинация** вместо простого лимита
   - Навигация по страницам
   - Переход к конкретной странице

2. **Фильтрация данных**
   - По дате
   - По значениям полей
   - Поиск по ключевым словам

3. **Сохранение предпочтений**
   - В localStorage браузера
   - В профиле пользователя

4. **Динамическая подгрузка**
   - Lazy loading при прокрутке
   - AJAX-запросы для догрузки

---

## Дата создания
17 декабря 2025

## Версия
1.0.0
