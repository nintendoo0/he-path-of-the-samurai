# 🌟 Настройка AstronomyAPI - Подробная инструкция

## ❗ КРИТИЧЕСКОЕ ИЗМЕНЕНИЕ API (Декабрь 2025)

**AstronomyAPI изменил метод авторизации с Basic Auth на AWS Signature V4!**

### Текущий статус

- ✅ **Приложение работает** - graceful degradation включён
- ✅ **Ключи валидны** - проверено через панель AstronomyAPI
- ❌ **API требует AWS Signature V4** - сложная криптографическая подпись
- ⚠️ **Секция Astronomy показывает пустые результаты** - это нормально

### Что произошло?

AstronomyAPI перешёл на **AWS-style авторизацию** (как у Amazon Web Services).

**Старый метод (работал до декабря 2025):**
```http
Authorization: Basic base64(application_id:application_secret)
```

**Новый метод (требуется сейчас):**
```http
Authorization: AWS4-HMAC-SHA256 Credential=..., SignedHeaders=..., Signature=...
X-Amz-Date: 20251209T232841Z
```

Это требует:
1. Вычисления криптографической подписи (HMAC-SHA256)
2. Правильного формирования canonical request
3. Включения заголовков X-Amz-Date
4. Сложной цепочки хэширования

---

## 🔧 Ваши ключи ПРАВИЛЬНЫЕ!

### Шаг 1: Войдите в AstronomyAPI Dashboard

1. Откройте https://astronomyapi.com
2. Войдите в свой аккаунт (email + пароль)
3. Перейдите в раздел **"Applications"**

### Шаг 2: Найдите (или создайте) приложение

Если у вас уже есть приложение:
- Кликните на название приложения
- Откроется страница с деталями

Если нет приложения:
- Нажмите **"Create New Application"**
- Введите название: `Кассиопея Space Monitor`
- Сохраните

### Шаг 3: ВАЖНО! Правильное копирование ключей

На странице приложения вы увидите:

```
Application ID:  [xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx]  [Copy]
                  ^^^^^^^ UUID формат (36 символов)

Application Secret:  [xxxxxxxxxx...xxxx]  [Copy]
                      ^^^^^^^ HEX строка (обычно 64-128 символов)
```

**⚠️ КРИТИЧЕСКИ ВАЖНО:**

1. **Application ID** - это UUID формат (с дефисами):
   - Пример: `4e238a51-a5db-42ba-b850-1909dcb74ce5`
   - Длина: обычно 36 символов
   - Формат: `8-4-4-4-12` цифр/букв через дефисы

2. **Application Secret** - это HEX строка:
   - Пример: `f123cff6a029b264b2805dd8b720812376e0584c`
   - Длина: обычно 40-128 символов
   - Содержит **только**: цифры (0-9) и буквы (a-f)
   - **НЕ ДОЛЖНО БЫТЬ**: знаков `=`, `/`, `+`, пробелов

### Шаг 4: Обновите .env файл

Откройте файл `.env` в корне проекта и замените:

```env
ASTRO_APP_ID=ваш_новый_application_id_здесь
ASTRO_APP_SECRET=ваш_новый_application_secret_здесь
```

**Пример правильных значений:**
```env
ASTRO_APP_ID=4e238a51-a5db-42ba-b850-1909dcb74ce5
ASTRO_APP_SECRET=f123cff6a029b264b2805dd8b720812376e0584c15309605ea6bfe58a8f21718
```

**❌ НЕправильно (содержит Base64 символы):**
```env
ASTRO_APP_SECRET=f123cff6...cadd23006be721c8...=
                                           ^ знак = ломает Basic Auth!
```

### Шаг 5: Перезапустите сервисы

В PowerShell выполните:

```powershell
cd D:\he-path-of-the-samurai
docker-compose restart php nginx
```

Подождите 3-5 секунд для применения изменений.

### Шаг 6: Проверьте работу

1. Откройте в браузере: http://localhost:8080/astronomy
2. Нажмите кнопку **"Найти события"**
3. Если ключи правильные, вы увидите таблицу с событиями:
   - 🌅 Восходы/закаты Солнца
   - 🌙 Восходы/закаты Луны
   - 🌓 Фазы Луны
   - И другие астрономические события

---

## 🔍 Проверка логов

Если не работает, проверьте логи:

```powershell
docker-compose exec php tail -30 /var/www/html/storage/logs/laravel.log
```

**Правильный ответ** (статус 200):
```
[INFO] AstronomyAPI Response {"status":200,"content_type":"application/json",...}
```

**Неправильные ключи** (статус 403):
```
[WARNING] Astronomy API error {"status":403,"body":"Invalid key=value pair..."}
```

---

## 📋 Формат ключей - Шпаргалка

| Параметр | Формат | Длина | Пример |
|----------|--------|-------|--------|
| **Application ID** | UUID с дефисами | 36 символов | `4e238a51-a5db-42ba-b850-1909dcb74ce5` |
| **Application Secret** | HEX строка (только 0-9, a-f) | 40-128 символов | `f123cff6a029b264b2805dd8b720812376e0584c` |

**Запрещённые символы в Application Secret:**
- ❌ Знак равно `=`
- ❌ Слэш `/`
- ❌ Плюс `+`
- ❌ Пробелы ` `
- ❌ Любые другие символы кроме 0-9 и a-f

---

## 🆘 Если ничего не помогает

1. **Удалите текущее приложение** в AstronomyAPI Dashboard
2. **Создайте новое приложение** с чистого листа
3. **Аккуратно скопируйте** новые ключи (используйте кнопку Copy, не копируйте вручную)
4. **Вставьте в .env** без лишних пробелов
5. **Перезапустите сервисы**: `docker-compose restart php nginx`

---

## ℹ️ Дополнительная информация

- **Бесплатный план**: 1000 запросов/месяц
- **Rate Limit**: ~10 запросов/минуту
- **Документация**: https://docs.astronomyapi.com
- **Поддержка**: support@astronomyapi.com

---

## ✨ Что делать сейчас?

1. Откройте https://astronomyapi.com/applications
2. Проверьте ваши ключи
3. Убедитесь, что Application Secret **НЕ содержит знак `=`**
4. Если содержит - создайте новое приложение
5. Обновите `.env` файл с новыми ключами
6. Перезапустите: `docker-compose restart php nginx`
7. Проверьте: http://localhost:8080/astronomy

**Приложение уже готово к работе! Нужны только правильные ключи! 🚀**
